1. Update SQLite query to use parametrized input and prevent SQL injection.
2. Store secret key in .env and use longer, more randomised string.
3. Remove logging of user credentials.
4. Implement password hashing.
5. Upgrade dependencies for known vulnerabilities (pip-audit)
6. Update 'Content-Security-Policy' response header: add "frame-ancestors 'none'; form-action 'self'" to protect against CSRF.
7. Update 'Cross-Origin-Embedder-Policy' response header: add "require-corp".
8. Set 'X-Content-Type-Options' response header to 'nosniff' to prevent older browsers sniffing harmful content marked as a different data type.
9. Set SameSite attribute for cookies to 'Lax' to reduce chance of CSRF.